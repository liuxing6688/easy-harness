# Harness 受控运行产物（见 `.codex/harness/spec/mechanical-gates.md` §8.4「受控运行产物例外」）：
# E2E 机读结果、Playwright trace/截图/video、QE 运行留痕产物均落盘于此，
# 由 e2e-run.mjs / qe-run.mjs / Playwright 进程内写盘生成，非门禁绕过。
test-results/
.harness/

# 工具链安装批准标记：含本机安装路径与用户确认摘要的临时授权状态（默认 60 分钟有效，
# 见 harness.config.json → toolchain），不应作为模板内容或跨机器/跨项目状态被提交。
.codex/toolchain-install-approved.json
.harness/.root-conversation-id.json
.harness/.dispatched-roles.json

# R34 证据产物执行证明的运行时状态：nonce 台账（公钥）与私钥交接目录。
# 属本机单次会话状态，绝不可提交——提交台账等于把可复用的 nonce 分发出去。
.harness/.exec-proof-ledger.json
.harness/.exec-proof-pending/

# R35 门禁异常事件旁路台账：Hook 独占写入的本机运行时状态，用于校验
# 「## 门禁异常事件」里的行确实是门禁自己写的。提交它等于把可复用的释放凭证分发出去。
.harness/.gate-exception-ledger.json

# 常见依赖/构建产物目录（按目标项目实际技术栈增补，非穷举；不影响门禁路径判定）
node_modules/
dist/
build/
.venv/
venv/
__pycache__/
target/

# 编辑器与系统噪音
.DS_Store
Thumbs.db
