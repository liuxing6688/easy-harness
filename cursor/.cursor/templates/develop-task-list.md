# 开发任务清单

## 1. 开发任务列表

> 「所属同构组」列须与 `detail-design-spec.md`「同构模块识别」章节的「同构组名称」一致；无同构组时填「无」。

| 任务包编号 | 关联需求 | 交付文件/目录 | 验收标准 | 测试类型 | 建议验证命令 | 所属同构组（如适用） |
| ---------- | -------- | -------------- | -------- | -------- | ------------ | --------------------- |
| T0-1 | R-001 | | | 单元/集成/E2E | | 无 |

## 2. 任务依赖关系

| 任务包编号 | 前置任务 | 后置任务 | 是否可并行 | 阻塞条件 |
| ---------- | -------- | -------- | ---------- | -------- |
| T0-1 | 无 | | 是/否 | |

## 3. 分派方式分析

### 3.1 阶段窗口

（按依赖关系划分的可并行批次窗口，例如：批次 1 = {T0-1, T0-2}，批次 2 = {T0-3}）

> **同构组前置任务包**：若某「同构组」下有 ≥2 个任务包，须先安排一个共享 primitive 任务包（编号建议 `T{n}-0` 或标注 `shared`），产出 `detail-design-spec.md`「同构模块识别」中声明的共享 primitive；该同构组下其余任务包须在 §2「前置任务」列引用此编号，不得在共享 primitive 就绪前并行「各自实现一份」。

### 3.2 整体分派模式

- 模式：`全串行` / `部分并行` / `全并行`（三选一，须与 §2 依赖关系吻合，不得凭空标注可并行）
- 关键路径：

### 3.3 并行窗口明细

| 批次 | 可并行任务包 | 分派开发线数 |
| ---- | ------------ | ------------ |
| 1 | | |
